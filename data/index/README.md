# Vector Index

I use this folder to store the generated FAISS vector index and serialized document records.

Files include:

- `faiss.index`
- `records.pkl`

These files are created locally and are excluded from Git tracking because they may contain information derived from private source documents.

Only this README file is included in the public repository.
